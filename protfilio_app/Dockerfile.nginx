# Use official nginx alpine image as base
FROM nginx:alpine

# Set working directory
WORKDIR /etc/nginx

# Copy custom nginx configuration file
COPY nginx.conf /etc/nginx/nginx.conf

# Copy static assets to the default HTML directory
COPY app/static /usr/share/nginx/html/static

# Expose port 80
EXPOSE 80
